# 前端实施规划

## 目标

提供一套适配桌面和手机的 Vue 网页，支持账号注册登录、监控会话管理，以及通过不可猜测 Token 查看实时纯标注。

## 技术栈

- Vue 3
- TypeScript
- Vite
- Pinia
- Vue Router
- 原生 WebSocket
- SVG 标注画布

## 页面规划

1. 登录页：用户名和密码登录。
2. 注册页：输入邀请码、用户名和两次密码；服务端开关关闭或邀请码错误时显示对应错误。
3. 控制台：创建新会话、复制和打开预览链接。
4. 预览页：地图平台、绳索、传送点、自己、队友和其他玩家。
5. 通知设置：控制台绑定 Bark DeviceKey，预览页逐项启停事件推送并发送测试通知。
6. 通知规则：每项事件独立开关，并允许配置规则所需的时间或数量阈值。
7. 极简预览：保留标准预览页，并提供 `/preview/:token/minimal` 窄屏模式；标准页可直接切换进入。

## 极简预览规划

- 用普通服务器存储监控页面作为外观，不使用卡片、色彩、阴影或装饰效果。
- 将 EXP 数值映射为“磁盘使用量”，经验百分比映射为“磁盘使用率”。
- 将 Bark 设置映射为“告警通道”“定时容量报告”“I/O 停滞告警”和“告警通道测试”。
- 标注地图显示为黑白“存储节点映射”，控制在 220px 左右并支持原生展开/收起；不显示额外外框，只保留内部线条区域。
- 顶部提供低调的模拟 CPU 使用率、1 分钟负载和持续滚动折线，用作普通服务器监控外观；节点映射默认折叠。
- 页面保持单列左对齐，在约 360px 的窄窗口中无横向滚动，适合作为低干扰监看条。
- 极简模式只改变展示名称与布局，继续复用标准模式的 WebSocket 数据和 Bark 接口。

## 实时策略

- 网页收到服务端快照后立即恢复最新地图与坐标。
- WebSocket 断开后按指数退避自动重连。
- 预览页面不需要账号登录，但必须持有随机预览 Token。
- 前端不接收和显示原始小地图图片。
- 预览页只读取渠道是否配置和规则状态，不读取 Bark DeviceKey。

## 后续

- 增加坐标插值动画。
- 增加预览链接主动撤销与再次生成。
- 增加多设备选择。
- HTTPS 上线后启用更严格的安全响应头。
