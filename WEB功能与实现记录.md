# AutoBuff Web 功能与实现记录

> 本文档是 Web 端功能、实现状态和后续改动的持续记录。新增页面、接口、权限、
> WebSocket 消息或重要交互时，应同步更新本文档和末尾的变更记录。

- 文档状态：持续维护
- 最近更新：2026-07-31
- 前端目录：`monitor-frontend`
- 配套后端目录：`monitor-backend`
- 客户端目录：`AutoBuff`

## 1. 当前整体状态

Web 端已经从“登录后直接进入单一监控页”升级为多功能控制台：

1. 用户登录后进入功能中心。
2. 远程监控只是功能中心的一个入口。
3. 所有用户都可以进入客户端管理，查看并控制自己账号下的客户端。
4. 超级管理员会额外看到用户管理入口。
5. 同一账号支持多台客户端，每台客户端拥有独立名称、状态和监控通道。

当前生产构建已通过。数据库升级和后端、客户端同时更新后，功能才能完整工作。

## 2. 技术栈与项目结构

### 2.1 技术栈

- Vue 3
- TypeScript
- Vite
- Pinia
- Vue Router
- 原生 WebSocket
- SVG 标注画布
- 原生 Fetch API

### 2.2 主要目录

| 路径 | 作用 |
| --- | --- |
| `src/router/index.ts` | Web 路由和登录拦截 |
| `src/stores/auth.ts` | 登录用户、令牌恢复和退出 |
| `src/api/client.ts` | HTTP 请求、Bearer Token 注入和错误处理 |
| `src/types/api.ts` | HTTP API 数据类型 |
| `src/types/protocol.ts` | 监控 WebSocket 数据协议 |
| `src/views/FunctionListView.vue` | 功能中心 |
| `src/views/ClientManagementView.vue` | 客户端管理 |
| `src/views/UserManagementView.vue` | 超级管理员用户管理 |
| `src/views/PreviewView.vue` | 标准监控和极简监控 |
| `src/views/DashboardView.vue` | Bark 通知设置 |
| `src/views/AuthView.vue` | 登录和注册 |
| `src/components/AnnotationStage.vue` | 地图结构和坐标标注画布 |
| `src/styles.css` | 全站样式和响应式布局 |

## 3. 页面与路由

| 路由 | 页面 | 权限 | 状态 |
| --- | --- | --- | --- |
| `/` | 自动跳转到功能中心 | 无 | 已完成 |
| `/login` | 登录 | 游客 | 已完成 |
| `/register` | 邀请码注册 | 游客 | 已完成 |
| `/functions` | 功能中心 | 登录用户 | 已完成 |
| `/clients` | 客户端管理 | 登录用户 | 已完成 |
| `/admin/users` | 用户管理 | 超级管理员 | 已完成 |
| `/admin/invite-codes` | 一次性限时邀请码管理 | 超级管理员 | 已完成 |
| `/admin/maps` | 云端地图管理 | 超级管理员 | 已完成 |
| `/dashboard` | 标准远程监控 | 登录用户 | 已完成 |
| `/dashboard/minimal` | 极简伪装监控 | 登录用户 | 已完成 |
| `/settings` | Bark 通知设置 | 登录用户 | 已完成 |
| `/preview/*` | 旧预览地址兼容跳转 | 无 | 已改为跳转功能中心 |

带有 `requiresAuth` 的路由会先检查当前会话是否有访问令牌。页面加载后还会调用
`GET /api/auth/me` 验证令牌和账号状态；令牌无效或账号被封禁时退出登录并返回登录页。

## 4. 账号与权限

### 4.1 登录

- 用户名和密码通过 `POST /api/auth/login` 提交；登录后的界面只展示昵称。
- 登录成功后将 JWT 保存到 `sessionStorage`。
- 当前标签页关闭后登录状态自动清除。
- 登录成功默认进入 `/functions`。
- 如果用户是被受保护页面重定向到登录页，登录成功后返回原目标路由。

### 4.2 注册

- 注册接口：`POST /api/auth/register`。
- 用户名限制为 3–32 位字母、数字或下划线。
- 昵称限制为 1–24 个字符，用于网页和客户端展示。
- 密码限制为 8–72 位。
- 需要超级管理员在网页生成的一次性限时邀请码，使用后立即核销；服务端忽略大小写和首尾空格。
- 服务端可通过 `ALLOW_REGISTRATION` 关闭注册。

### 4.3 超级管理员

用户数据包含：

```ts
interface User {
  id: number
  nickname: string
  isSuperAdmin: boolean
}
```

超级管理员权限来自数据库 `users.is_super_admin` 字段：

- `0`：普通用户。
- `1`：超级管理员。
- Web 端不提供修改该字段的页面或接口。
- 必须由有数据库权限的运维人员手动修改。

示例：

```sql
UPDATE users
SET is_super_admin = 1
WHERE username = 'your_admin_username';
```

前端隐藏入口只是界面层处理。真正的权限校验由后端对每一个管理员 API 再次执行，
普通用户即使手动访问路由或构造请求也不能操作用户管理功能。

## 5. 功能中心

实现文件：`src/views/FunctionListView.vue`

登录后首页不再直接打开监控，而是展示功能入口卡片：

### 5.1 远程监控

- 所有登录用户可见。
- 进入标准监控页面。
- 用于查看地图结构、玩家位置、经验状态和告警信息。

### 5.2 客户端管理

- 所有登录用户可见。
- 进入当前账号的客户端列表。
- 使用纵向单列卡片查看状态，并支持远程开始、停止和解绑；监控入口统一保留在功能中心。

### 5.3 用户管理

- 仅 `isSuperAdmin === true` 时显示。
- 进入全站用户管理页面。
- 普通用户直接访问 `/admin/users` 会被页面送回功能中心，同时后端接口仍会返回
  `403`。

### 5.4 地图管理

- 仅 `isSuperAdmin === true` 时显示功能入口。
- 支持查看地图名称、大小、上传者昵称和更新时间，并按需渲染地图结构预览。
- 支持上传客户端导出的 JSON、下载单张地图和删除云端地图。
- 普通用户直接访问 `/admin/maps` 会被送回功能中心，后端接口仍会返回 `403`。

## 6. 客户端管理

- 每个账号只允许保存一个挂绳队伍；已有队伍时入口显示“修改队伍”。
- 已创建的队伍支持“解散队伍”。确认后服务端仅向在线队长发送解散指令，队长客户端在游戏聊天框拟人输入 `/退出隊伍`，随后网页移除队伍状态。

实现文件：`src/views/ClientManagementView.vue`

### 6.1 客户端身份

每台 AutoBuff 客户端在本机生成并持久化一个随机 UUID。UUID 只用于系统内部识别，
不会作为主要名称显示给用户。

客户端第一次连接后，服务端生成一个全局不重复的趣味名称，例如：

- `追风水獭-128`
- `星尘狐狸-064`
- `薄荷企鹅-512`

服务器将名称通过设备 WebSocket 返回，客户端保存在本地。网页和 AutoBuff 主界面
均显示服务端分配的同一个名称，方便用户确认对应设备。

### 6.2 列表字段

客户端卡片显示：

- 趣味名称。
- 是否在线。
- 当前模式。
- 当前是否正在运行。
- 最后连接时间。
- 查看该客户端监控的入口。
- 开始或停止按钮。

当前模式映射：

| 协议值 | 页面显示 |
| --- | --- |
| `dead` | 死花模式 |
| `live` | 活花模式 |
| `follow_heal` | 跟补模式 |
| `monitor` | 监控模式 |

### 6.3 实时连接

网页使用：

```text
GET /ws/clients?access_token=...
```

连接建立后，服务端推送完整客户端列表：

```json
{
  "type": "clients",
  "clients": [
    {
      "id": "服务端会话 ID",
      "clientId": "客户端本机 ID",
      "name": "追风水獭-128",
      "mode": "monitor",
      "running": true,
      "online": true,
      "lastSeenAt": 1785430800000
    }
  ]
}
```

客户端上线、离线或状态变化时，服务端重新推送列表。WebSocket 断开后，网页每两秒
自动尝试重连。

### 6.4 网页控制开始和停止

网页向 `/ws/clients` 发送：

```json
{
  "type": "command",
  "clientId": "客户端本机 ID",
  "action": "start"
}
```

或：

```json
{
  "type": "command",
  "clientId": "客户端本机 ID",
  "action": "stop"
}
```

后端先验证该客户端属于当前登录账号，再将命令转发到对应设备 WebSocket。

客户端收到开始命令后仍会执行本机已有校验，包括：

- 是否已经选择并识别游戏窗口。
- 游戏窗口是否仍然有效。
- 当前模式需要的辅助功能权限。
- 当前模式需要的屏幕录制权限。
- 技能、按键和模式配置是否合法。

校验失败时不会强行运行，客户端会继续上报 `running: false`。网页按钮会等待新的
客户端状态，并根据最终状态恢复。

## 7. 远程监控

实现文件：`src/views/PreviewView.vue`

### 7.1 指定客户端

从客户端管理进入监控时使用：

```text
/dashboard?client=<clientId>
```

网页查看端 WebSocket 会携带相同的客户端 ID：

```text
/ws/view?access_token=...&client_id=...
```

后端验证该客户端属于当前用户后，才允许订阅它的监控房间。

如果没有提供 `client` 参数，后端默认选择当前账号最后连接的有效客户端，兼容从
功能中心直接进入监控的操作。

### 7.2 标准监控页面

当前支持显示：

- 客户端在线和监控通道状态。
- 当前地图名称。
- 平台、绳索和传送点。
- 当前角色、队友和其他玩家坐标。
- 原始采集 FPS。
- 当前 EXP。
- 经验百分比。
- EXP 识别置信度。
- 经验停滞状态。
- 符文诅咒提示状态和置信度。
- 安全区范围和越界状态。
- 近 10 分钟、近 1 小时、总计和当日经验增量统计。
- Bark 告警开关和测试。

网页不接收原始游戏截图，只接收地图结构、归一化坐标和识别结果。

### 7.3 查看端 WebSocket 消息

首次连接会收到 `snapshot`，用于立即恢复最近状态：

```json
{
  "type": "snapshot",
  "online": true,
  "map": {},
  "frame": {},
  "status": {},
  "exp": {},
  "rune": {},
  "zone": {},
  "gain": {},
  "updatedAt": 1785430800000
}
```

后续增量消息类型：

| 类型 | 内容 |
| --- | --- |
| `map` | 地图结构 |
| `frame` | 玩家与其他标记坐标 |
| `status` | 监控文本状态 |
| `exp` | 当前 EXP 和识别结果 |
| `rune` | 符文提示状态 |
| `zone` | 安全区和越界状态 |
| `gain` | 服务端汇总后的经验增量 |

查看端断线后按指数退避自动重连。

## 8. 极简监控

路由：`/dashboard/minimal`

极简监控与标准监控复用同一套数据、WebSocket 和告警接口，只改变显示方式。页面
伪装为普通服务器存储监控：

| 页面文案 | 实际含义 |
| --- | --- |
| 磁盘使用量 | 当前 EXP |
| 磁盘使用率 | 经验百分比 |
| 磁盘写入速率 | 每 5 秒经验增量 |
| 流入流量 | 近 10 分钟经验增量 |
| 流出流量 | 近 1 小时经验增量 |
| 总资源包用量 | 历史总经验增量 |
| 当日资源包用量 | 当日经验增量 |
| 写入锁定 | 符文提示 |
| 分区越界 | 角色离开安全区 |
| I/O 停滞 | EXP 长时间没有变化 |

其他已实现功能：

- 黑白线稿界面。
- 深色和浅色主题切换。
- 主题选择保存在 `localStorage`。
- 模拟 CPU 使用率和负载折线。
- 存储节点映射默认折叠。
- 适配约 360px 宽的窄窗口。

完整文案对照见 `极简模式黑话对照表.md`。

## 9. Bark 通知设置

实现文件：

- `src/views/DashboardView.vue`
- `src/views/PreviewView.vue`

支持：

- 绑定和更新 Bark DeviceKey。
- 显示是否已经配置通知通道。
- 发送测试通知。
- 独立启停经验停滞告警。
- 设置经验停滞阈值，范围 10–86400 秒。
- 独立启停符文提示告警。
- 独立启停安全区越界告警。

出于安全考虑，网页只读取“是否已经配置”，不会从服务端读回 Bark DeviceKey
明文。

## 10. 用户管理

实现文件：`src/views/UserManagementView.vue`

### 10.1 用户列表

接口：

```text
GET /api/admin/users
```

显示：

- 用户 ID。
- 昵称（用户名仅用于登录，不在管理界面展示）。
- 普通用户或超级管理员身份。
- 正常或已封禁状态。
- 已登记客户端数量。
- 最近登录时间。

### 10.2 封禁和解封

接口：

```text
PATCH /api/admin/users/{id}/status
```

请求：

```json
{
  "status": 0
}
```

- `0` 表示封禁。
- `1` 表示解封。
- 当前登录的超级管理员不能在网页中封禁自己。
- 被封禁用户后续 HTTP 请求和新 WebSocket 连接会被后端拒绝。

### 10.3 修改密码

接口：

```text
PUT /api/admin/users/{id}/password
```

请求：

```json
{
  "password": "new-password"
}
```

新密码长度限制为 8–72 位，密码哈希由后端使用 bcrypt 生成，前端和数据库均不
保存密码明文。

## 11. HTTP API 汇总

| 方法 | 地址 | 用途 | 权限 |
| --- | --- | --- | --- |
| `POST` | `/api/auth/login` | 登录 | 游客 |
| `POST` | `/api/auth/register` | 注册 | 游客 |
| `GET` | `/api/auth/me` | 恢复当前用户 | 登录用户 |
| `GET` | `/api/clients` | 获取客户端列表的 HTTP 版本 | 登录用户 |
| `DELETE` | `/api/clients/{sessionId}` | 解绑当前账号自己的客户端 | 登录用户 |
| `GET` | `/api/admin/users` | 获取所有用户 | 超级管理员 |
| `PATCH` | `/api/admin/users/{id}/status` | 封禁或解封 | 超级管理员 |
| `PUT` | `/api/admin/users/{id}/password` | 修改用户密码 | 超级管理员 |
| `GET` | `/api/admin/maps` | 获取云端地图列表 | 超级管理员 |
| `POST` | `/api/admin/maps` | 上传或覆盖地图 | 超级管理员 |
| `GET` | `/api/admin/maps/{id}` | 下载单张地图 | 超级管理员 |
| `DELETE` | `/api/admin/maps/{id}` | 删除云端地图 | 超级管理员 |
| `GET` | `/api/notifications/bark` | 获取 Bark 设置状态 | 登录用户 |
| `PUT` | `/api/notifications/bark` | 保存 Bark 设置 | 登录用户 |
| `POST` | `/api/notifications/bark/test` | 测试 Bark | 登录用户 |
| `PUT` | `/api/notifications/exp-stalled` | 设置经验停滞告警 | 登录用户 |
| `PUT` | `/api/notifications/rune-alert` | 设置符文告警 | 登录用户 |
| `PUT` | `/api/notifications/zone-breach` | 设置越界告警 | 登录用户 |
| `GET` | `/api/monitor/exp-gain` | 获取经验增量统计 | 登录用户 |
| `POST` | `/api/monitor/exp-gain/reset-total` | 清空历史总增量 | 登录用户 |

## 12. WebSocket 汇总

| 地址 | 连接方 | 用途 |
| --- | --- | --- |
| `/ws/device?client_id=...` | AutoBuff 客户端 | 上报状态、接收开始/停止命令、接收设备名称 |
| `/ws/clients?access_token=...` | 客户端管理页 | 接收设备列表、发送开始/停止命令 |
| `/ws/view?access_token=...&client_id=...` | 监控页面 | 接收指定客户端监控快照和增量 |

WebSocket 在 HTTP 环境使用 `ws://`，HTTPS 环境自动切换为 `wss://`。

## 13. 数据库依赖

本轮功能依赖：

```text
monitor-backend/migrations/client_management_migration.sql
```

迁移增加：

- `users.is_super_admin`
- `monitor_sessions.client_id`
- `monitor_sessions.mode`
- `monitor_sessions.running`
- 用户与客户端 ID 唯一索引
- 客户端趣味名称全局唯一索引

已有数据库必须先执行迁移，再启动新后端和新客户端。

## 14. 状态存储

### 14.1 浏览器

| 数据 | 存储位置 | 生命周期 |
| --- | --- | --- |
| JWT | `sessionStorage` | 当前浏览器标签页会话 |
| 极简页深色主题 | `localStorage` | 长期保留 |
| 客户端列表 | Vue 内存状态 | 页面生命周期 |
| 监控快照 | Vue 内存状态 | 页面生命周期 |

### 14.2 AutoBuff 客户端

客户端本机文件 `remote-monitor.json` 保存：

- 登录令牌。
- 持久化客户端 UUID。
- 服务端分配的客户端趣味名称。

退出账号时只删除登录令牌，保留客户端 UUID 和名称，避免同一台机器重新登录后被
识别成一台全新的设备。

## 15. 安全边界

- 所有业务 API 都由后端验证 JWT 和账号启用状态。
- 管理员 API 同时验证 `is_super_admin`。
- 客户端列表只查询当前用户的数据。
- 下发命令前，后端再次验证客户端归属。
- 指定监控客户端时，后端再次验证客户端归属。
- 超级管理员字段没有 Web 修改接口。
- 服务端只保存 bcrypt 密码哈希；启用 HTTPS 后，密码请求体才具备公网传输保护。
- Bark DeviceKey 加密保存在后端，Web 不读取明文。
- 监控链路不上传原始游戏画面。

生产环境仍应尽快启用 HTTPS/WSS，避免令牌和密码在公网明文链路上传输。

## 16. 响应式和交互状态

当前功能中心、客户端管理和用户管理采用同一套 `portal-*` 样式：

- 桌面端使用双列功能卡片和客户端卡片。
- 小于 720px 时自动切换单列。
- 用户表格在窄屏下允许横向滚动。
- 在线、运行、管理员和封禁状态使用独立视觉标记。
- 离线客户端禁用开始/停止按钮。
- 命令发出后按钮显示“等待客户端…”，收到状态更新后恢复。

标准监控页和极简监控页继续使用各自已有的响应式布局。

## 17. 构建和验证

### 17.1 开发

```bash
npm install
npm run dev
```

Vite 开发服务器会将 `/api` 和 `/ws` 代理到 `127.0.0.1:28672`。
使用 `npm run dev:online` 时，代理目标切换为线上服务；本地页面操作会真实读写
线上数据库。

### 17.2 生产构建

```bash
npm run build
```

要求 Node.js 20.19+ 或 22.12+。构建产物位于 `dist/`。

本轮验证结果：

- Vue TypeScript 类型检查通过。
- Vite 生产构建通过。
- Go 后端测试通过。
- Go 实时通信测试及 race 检查通过。
- AutoBuff macOS Debug 构建通过。

## 18. 当前已知限制与后续事项

- [ ] 生产环境升级数据库并部署新后端、Web 静态文件和 AutoBuff 客户端。
- [ ] 使用真实多客户端账号完成端到端联调。
- [ ] 对功能中心、客户端管理和用户管理做正式浏览器视觉验收。
- [ ] 上线 HTTPS/WSS。
- [ ] 管理员修改密码后使用户旧 JWT 立即失效。
- [ ] 封禁用户时主动断开其已经建立的 WebSocket，而不只拒绝后续请求和重连。
- [ ] 为远程开始失败增加结构化失败原因，并直接显示在网页客户端卡片上。
- [ ] 增加客户端重命名、移除和历史操作记录。
- [ ] 增加用户列表搜索、分页和操作审计。

## 19. 变更记录

### 2026-07-31：修复多客户端统计与通知会话选择

- 修复同一账号存在多个会话时，经验累计基准反复重置导致四项用量始终为 0。
- 后端改为按客户端会话分别维护 EXP 基准，再汇总到账户级流量和用量。
- 经验统计只处理已登记、处于监控模式且正在运行的客户端。
- I/O 停滞、写入锁定和分区越界通知不再遍历旧版遗留会话。
- 多台监控客户端同时在线时，通知服务优先使用实际在线且数据有效的会话。

### 2026-07-31：功能中心、多客户端和超级管理员

- 登录后首页由监控页改为功能中心。
- 新增客户端管理入口。
- 新增超级管理员专属用户管理入口。
- 新增多客户端身份、趣味名称和在线状态。
- 新增客户端模式和运行状态同步。
- 新增 WebSocket 远程开始/停止。
- 监控页面支持指定客户端。
- 新增用户封禁、解封和密码修改。
- 新增数据库超级管理员字段，且只能手动修改。
- 新增本文档，后续 Web 改动持续记录。

### 既有功能

- 完成账号登录、邀请码注册和 JWT 恢复。
- 完成标准纯标注监控。
- 完成极简伪装监控和深浅主题。
- 完成 EXP、符文、安全区和经验增量展示。
- 完成 Bark 配置、测试和三类告警规则。
- 完成桌面与窄屏响应式布局。
