# 极简模式黑话对照表

极简预览页（地址形如 `/preview/你的token/minimal`）故意打扮成「服务器存储监控」，方便在公共场合打开。

**页面上写的是黑话，真实含义看右边。**

浏览器标题也会改成：`服务器存储监控`。

---

## 一句话总览

| 整页在演什么 | 实际在监控什么 |
| --- | --- |
| 一台服务器的磁盘 / 流量 / 告警 | Maple 跟补：经验、符文、安全区、手机推送 |

---

## 顶部与装饰

| 页面黑话 | 真实含义 | 备注 |
| --- | --- | --- |
| 服务器存储监控 | 极简版监控首页标题 | 纯伪装文案 |
| 节点状态：在线 / 等待数据 / 正在连接 | AutoBuff 本机是否连上监控服务 | 和标准版「在线」同一回事 |
| 深色主题 | 黑底白字开关 | 真功能，选中会记住 |
| CPU 使用率 | **假数据** | 浏览器随机晃动的折线，看着像服务器负载 |
| 1 分钟负载 | **假数据** | 由上面假 CPU 换算出来的数字 |
| 存储节点映射 | 小地图 + 角色位置标注 | 点开才能看到；框是安全区 |

---

## 经验相关（磁盘 / 流量 / 资源包）

经验读数在极简页里统一伪装成「磁盘 / 流量」。单位写成 `MB`，数字其实就是经验值。

| 页面黑话 | 真实含义 | 怎么算的 |
| --- | --- | --- |
| 磁盘使用量 | 当前经验值 `currentEXP` | 本机识别到多少就显示多少 |
| 磁盘使用率 | 当前经验条百分比 | 例如 `42.125%` |
| 磁盘写入速率 | 最近 **5 秒**拿到的经验增量 | 网页自己每 5 秒采一次；曲线按峰值自动缩放 |
| 平均写入 | 曲线窗口内的平均每秒增量 | 同样是经验，不是真磁盘 |
| 流入流量 | 最近 **10 分钟**获得的经验总量 | 服务端累计后推送（`gain.inflow10m`） |
| 流出流量 | 最近 **1 小时**获得的经验总量 | 服务端累计后推送（`gain.outflow1h`） |
| 总资源包用量 | 从开监控以来**跨多次启动**累计拿到的经验 | 服务端落库；可手动清空 |
| 当日资源包用量 | **当天**拿到的经验 | 北京时间每天 0 点清零；服务端落库 |
| 清空总资源包用量 | 把「总资源包用量」归零 | **只清总累计**；当日和 10 分/1 小时流量不动 |

补充规则（和标准监控一致）：

- 升级或识别抖动导致经验**变少**时，增量按 **0** 算，不会把累计往下扣。
- **读不到经验**时：不算「正在获得经验」，也不算「I/O 停滞」。

---

## 状态灯（符文 / 安全区 / 停滞）

| 页面黑话 | 显示值 | 真实含义 |
| --- | --- | --- |
| 写入锁定 | 正常 / 已锁定 | 画面上有没有紫色**符文诅咒**提示 |
| 分区越界 | 正常 / 已越界 / 未划分区 | 角色有没有跑出**安全区**矩形；没设安全区就是「未划分区」 |
| I/O 停滞 | 正常 / 已停滞 / 等待读数 | 经验是否长时间**不涨**（网页按「最后变化时间」自己算） |

地图上的黑色虚线框 = 安全区范围；越界时框的语义仍是「出区了」。

---

## 告警（Bark 手机推送）

| 页面黑话 | 真实含义 | 推送规则 key |
| --- | --- | --- |
| 告警通道 | 有没有配好 Bark（手机推送） | — |
| 写入锁定告警 | 符文出现时推手机 | `rune_alert`（约每 5 秒一次，直到解除） |
| 分区越界告警 | 离开安全区时推手机 | `zone_breach`（约每 5 秒一次，直到回区） |
| I/O 停滞告警 | 经验长时间不涨时推手机 | `exp_stalled` |
| 检测间隔（秒） | 经验多久不涨才算停滞、以及重复提醒间隔 | 只影响「I/O 停滞告警」 |
| 测试告警通道 | 发一条 Bark 测试通知 | 用来确认手机能收到 |

登录控制台里绑定的 DeviceKey，在极简页**不会显示**（防止泄密）。

---

## 和标准预览页怎么对应

标准页说人话，极简页说黑话，数据是同一套：

| 标准预览页 | 极简预览页 |
| --- | --- |
| 经验数据 / 当前 EXP | 磁盘使用量 |
| 经验进度 | 磁盘使用率 |
| （无单独卡片，有写入曲线） | 磁盘写入速率 |
| （服务端 `gain`） | 流入 / 流出流量、总/当日资源包用量 |
| 符文诅咒 | 写入锁定 |
| 安全区 / 已离开 | 分区越界 |
| 经验无增长提醒 | I/O 停滞告警 |
| Bark 推送 | 告警通道 |

入口：标准页右上角点「极简版」；地址多一段 `/minimal`。

---

## 哪些是假的，哪些是真的

| 类型 | 有哪些 |
| --- | --- |
| **纯装饰假数据** | CPU 使用率、1 分钟负载 |
| **真数据换马甲** | 磁盘用量/利用率、写入速率、流量、资源包、写入锁定、分区越界、I/O 停滞、告警开关 |
| **真界面功能** | 深色主题、展开地图、清空总资源包、改检测间隔、测试告警 |

---

## 相关代码位置（给以后改名用）

- 页面文案：`src/views/PreviewView.vue`（`minimalMode` 分支）
- 流量 / 资源包协议：`src/types/protocol.ts` → `GainPayload`
- 后端累计与落库：`monitor-backend/internal/expgain/`
- 建表：`exp_gain_migration.sql`
